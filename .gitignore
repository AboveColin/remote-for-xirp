/xirp-remote
*.log
.DS_Store

# Android wrapper build output and signing material, which must never be committed.
/deploy/android/twa/
*.keystore
*.jks

# graft's local graph cache. Regenerable, so it is not committed: run `graft build`.
graft/
